% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{init}
\alias{init}
\title{Initialize an omopHeor study (Stage 1: Cohort Generation)}
\usage{
init(cdm, target_cohort, comparator_cohort, outcome_cohort)
}
\arguments{
\item{cdm}{A \code{cdm_reference} object created by \code{CDMConnector::cdmFromCon()}.}

\item{target_cohort}{A string specifying the name of the target cohort table in the CDM.}

\item{comparator_cohort}{A string specifying the name of the comparator cohort table in the CDM.}

\item{outcome_cohort}{A string specifying the name of the outcome cohort table in the CDM.}
}
\value{
An \code{omopheor_study} (\code{hermes_study}) S3 object containing the CDM reference and cohort metadata,
ready to be piped into \code{summarise_baseline()}.
}
\description{
\code{init()} is the entry point for the omopHeor 6-stage pipeline. It takes an existing
OMOP Common Data Model (CDM) reference and the names of pre-generated cohort tables,
validating their existence and calculating baseline cohort counts.

In the context of Health Economics and Outcomes Research (HEOR), this step maps
directly to defining the \strong{Treatment Arm} (\code{target_cohort}), the \strong{Standard of Care Arm}
(\code{comparator_cohort}), and the clinical event or \strong{Health State} of interest (\code{outcome_cohort}).
}
\seealso{
\code{vignette("intro-to-heor")} for a mapping between OMOP cohorts and HEOR concepts.
}
